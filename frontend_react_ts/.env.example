# ============================================================
# FORGE BY MODULA PROJECT
# FRONTEND ENVIRONMENT CONFIGURATION (.env.example)
# ============================================================
# Copy this file to .env or .env.local in frontend_react_ts/.
#
# CRITICAL SECURITY NOTE:
# Frontend MUST NOT contain Gemini/Google AI keys or any private server secrets!
# All AI calls are mediated securely by the Rails backend.

# Backend API base URL
# Local development: empty (uses Vite proxy) or http://127.0.0.1:3000
# Production (Vercel): URL of your Railway backend, e.g. https://forge-backend.up.railway.app
VITE_API_BASE_URL=http://127.0.0.1:3000
